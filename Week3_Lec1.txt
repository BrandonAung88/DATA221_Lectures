# Daily Edit → Upload Flow

1. git pull origin main
2. [EDIT FILES - Jupyter notebooks]
3. git add .
4. git status
5. git commit -m "Improve data handling for better API performances" "Verb,describe what commit does,describe why commit is necessary"
6. git push origin main

❌ dquote> prompt → Type " and Enter (or Ctrl+C)
❌ src refspec master → Use "main" instead of "master"
❌ Auth failed → Use GitHub Personal Access Token as password
✅ After first push → No more login prompts

Daily Edit → Upload Flow
